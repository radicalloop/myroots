# Family Tree Frontend

React + Vite frontend with React Flow + Dagre tree visualization.

## Setup

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

App runs at `http://localhost:5173`.

## Environment

```
VITE_API_BASE_URL=http://localhost:3001/api
```
